apiVersion: v1
kind: Service

metadata:
  name: frontend-service
  namespace: todo

spec:
  type: NodePort

  selector:
    app: todo-frontend

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080